export default interface Contract {
    company: string;
    firstName: string;
    lastName: string;
    project: string;
    startDate: string;
    endDate?: Date;
    status: string;
    git: string;
}